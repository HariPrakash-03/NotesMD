package com.notesmd.core.model

/**
 * Closed set of font family choices for the Template Studio.
 * Applies to prose (body/heading) nodes only — code blocks always use JetBrainsMono.
 */
enum class FontFamilyOption {
    SANS_SERIF,
    SERIF,
    MONOSPACE
}
